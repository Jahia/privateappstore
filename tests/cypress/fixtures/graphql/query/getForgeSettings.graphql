query ForgeSettings($siteKey: String!) {
    forge {
        settings(siteKey: $siteKey) {
            siteKey
            url
            id
            user
            passwordSet
        }
    }
}
