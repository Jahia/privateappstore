query GetNodeProperty($path: String!, $name: String!, $language: String) {
    jcr(workspace: EDIT) {
        nodeByPath(path: $path) {
            uuid
            path
            name
            property(name: $name, language: $language) {
                name
                value
                values
            }
        }
    }
}
